name: 📝 Format

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  release:
    uses: grovegs/workflows/.github/workflows/godot-package-format.yml@main
    with:
      core-project: src/GroveGames.BehaviourTree
      godot-project: src/GroveGames.BehaviourTree.Godot
      core-test-project: tests/GroveGames.BehaviourTree.Tests
      godot-test-project: tests/GroveGames.BehaviourTree.Godot.Tests
      global-json-file: global.json
